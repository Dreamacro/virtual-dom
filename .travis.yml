dist: xenial
sudo: required
language: node_js
node_js: stable
cache:
  yarn: true
  directories:
    - node_modules
addons:
   apt:
      sources:
         - google-chrome
      packages:
         - google-chrome-stable
before_script:
  - yarn
  - google-chrome --version
script:
  - yarn run test